import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:keep_up/app/modules/login/modules/recovery_pass/recovery_pass_page.dart';

void main() {
  testWidgets('RecoveryPassPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(RecoveryPassPage(title: 'RecoveryPass')));
    //  final titleFinder = find.text('RecoveryPass');
    //  expect(titleFinder, findsOneWidget);
  });
}
